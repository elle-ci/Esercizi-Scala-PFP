# Esercitazi Scala PFP
Alcuni esercizi in linguaggio Scala svolti durante le esercitazioni del corso di programmazionef unzionale e parallela. 

I file main contengono i test dell'esercizio, per compialre un'esercizio spostarsi all'interno della dirctory in cui è contenuto (es. E1) e lanciare il comando <code>scalac E1MAin.scala E1.scala</code>.
Per eseguire il test sul file usare <code>scala E1Main</code>.
